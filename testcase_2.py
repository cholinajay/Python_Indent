def manhattan_distance(start, end):
        sx, sy = start
        ex, ey = end
        def fun(a):
                a=20
                a=10
                return a
        return abs(ex - sx) + abs(ey - sy)
